export {};

let name = 'Tom';

// let length = name.length as number;
let length = (name as string).length;

// Deprecated because it resembles JSX notation
// let length = (<string>name).length;

// length = 'foo';
